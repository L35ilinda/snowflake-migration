# 2026-04-23 — v0.2.0 ship; stacked-PR lesson; v0.3.0-serve scaffold

Three things shipped: the v0.2.0-model-the-warehouse tag landed via two
PRs, the stacked-PR misadventure was captured as ADR-0016, and v0.3.0-serve
was scaffolded down to "GUI work pending."

## v0.2.0 shipped

- [x] Two commits made locally:
  - `7d6f5bc` — Replicate Sources finalisation (snowpipe `_LOADED_AT` fix, quarantine module, mock-ops scaffold, scope lock to 3 tenants)
  - `28ab685` — v0.2.0-model-the-warehouse (Type 2 `dim_policy` + Data Vault on transactions)
- [x] Branches created and pushed:
  - `replicate-sources/finalisation` (at `7d6f5bc`)
  - `core/vault-and-type2` (at `28ab685`)
- [x] Stacked PRs opened: #9 (base `master`) and #10 (base `replicate-sources/finalisation`).
- [x] **PR #9 merged with `--delete-branch`** (CI: dbt 40s + terraform 27s, both green). Merge commit `c2ba852`.
- [x] **PR #10 auto-closed by GitHub** when its base branch ceased to exist. Tried `gh pr reopen` + `gh pr edit --base master` — both rejected ("Cannot change the base branch of a closed pull request").
- [x] **Recovery:** opened PR #11 from `core/vault-and-type2` → `master` with the same body, referencing closed #10. CI green (47s). Merged with `--delete-branch`. Merge commit `f5d9d40`.
- [x] Local `master` reset to `origin/master`. Stale local tag `v0.2.0-model-the-warehouse` (had pointed at `28ab685`) deleted.
- [x] **Tag `v0.2.0-model-the-warehouse` re-created at `f5d9d40`** (the merge commit) and pushed.

## ADR-0016 — stacked-PR merge convention

- [x] Wrote [ADR-0016](../adr/0016-stacked-pr-merge-convention.md). Decision:
  **retarget downstream PR base to `master` BEFORE deleting the upstream
  branch.** Four-line `gh` recipe documented as canonical. Stacked-PR
  tooling (`spr`, Graphite) explicitly deferred until stack depth or
  frequency justifies it.
- [x] Cross-referenced under a new "GitHub workflow" section in
  [issues-and-fixes.md](issues-and-fixes.md) so anyone replicating the
  project hits the recipe without reading the ADR.

## v0.3.0-serve scaffold

- [x] [ADR-0017](../adr/0017-power-bi-serving.md) — three decisions:
  1. **Semantic model lives in Power BI** for v0.3.0. Snowflake Semantic
     Views flagged as v0.3.x stretch (would land as ADR-0018). dbt
     Semantic Layer rejected (dbt Cloud only).
  2. **DirectQuery for the semantic model** (warehouse-native pushdown,
     bounded by `RM_BI_WH` 2-credit cap which IS the FinOps demo).
     **Import for the paginated report** (one render per parameterized
     run; no interactive re-querying).
  3. **OAuth as `LSILINDA` for build phase**; **`PBI_SVC` key-pair for
     publish**. Microsoft Entra service principal flagged as future
     reversal trigger when shared-credential rotation matters.
- [x] **Snowflake side via Terraform:** `PBI_SVC` user (key-pair,
  `default_role = FR_ANALYST`, `default_warehouse = BI_WH`) + out-of-module
  grant (`FR_ANALYST` to `PBI_SVC`). No schema or warehouse changes —
  `FR_ANALYST` already has marts_ro and `BI_WH` USAGE.
- [x] `terraform plan` — exactly 2 adds. `terraform apply` — clean.
- [x] **Repo scaffold:** [power_bi/README.md](../../power_bi/README.md)
  documenting the semantic model design (3 facts, 6 dims, 9 relationships,
  9 measures), cost expectations (DirectQuery → expect cap to trip),
  status checklist.
- [x] Four walkthroughs (the publish one was deleted later in the session — see scope cut below):
  - [01_connect.md](../../power_bi/walkthrough/01_connect.md) — Power BI
    Desktop connection details (server / warehouse / role / database;
    DirectQuery; Microsoft Account auth)
  - [02_semantic_model.md](../../power_bi/walkthrough/02_semantic_model.md) —
    table renames, mark-as-date, relationships table, hierarchy build,
    full DAX for 9 measures
  - [03_paginated_report.md](../../power_bi/walkthrough/03_paginated_report.md) —
    Power BI Report Builder; ODBC connection; advisor commission
    statement query + parameters + layout
  - `04_publish.md` — Power BI Service workflow to swap from `LSILINDA`
    OAuth to `PBI_SVC` key-pair via cloud connection. **Deleted later
    in the session** when publish was scoped out.
- [x] **Shipped via PR #12** — branch `serve/v0.3.0-scaffold` → `master`. dbt CI 48s, terraform fmt+validate 25s, both green. Merge commit `75bb4ff`.

## v0.3.0 publish-step scope cut (later in same session)

- [x] Decided to **skip Power BI Service publish entirely** for v0.3.0. Rationale: the `.pbix` + `.rdl` + screenshots in the repo are sufficient portfolio evidence; publishing requires a Pro license and adds operational surface area for zero portfolio benefit.
- [x] [ADR-0017](../adr/0017-power-bi-serving.md) gains a 2026-04-22 addendum at the top recording the scope cut. §1 (semantic model in Power BI) and §2 (DirectQuery + Import) decisions are unaffected; only §3 publish phase is retired.
- [x] **Deleted `power_bi/walkthrough/04_publish.md`** — dead instructions.
- [x] **Trimmed `power_bi/README.md`** — dropped publish references; status checklist reflects `LSILINDA` as the only Power BI connection identity.
- [x] **`PBI_SVC` Snowflake user destroyed via Terraform.** It existed solely as the published-model service identity; with publish skipped, it's dead code (unlike `RAW_OPS` which is genuinely deferred to v1.1.0). `terraform plan` showed exactly 2 destroys (user + grant); apply clean.
- [x] **Cleanup:** removed `pbi_svc_public_key_path` from [variables.tf](../../terraform/environments/dev/variables.tf) + [terraform.tfvars](../../terraform/environments/dev/terraform.tfvars). Inline `PBI_SVC` block in [environments/dev/main.tf](../../terraform/environments/dev/main.tf) replaced with a tombstone comment so the removal is grep-able.
- [x] **Key files left on disk** at `C:/Users/Lonwabo_Eric/.snowflake/keys/pbi_svc_rsa_key.{p8,pub}` for the user to delete manually if desired.
- [x] CLAUDE.md updated: Done line for Power BI scaffold rewritten (drops `PBI_SVC`, notes publish-skip); §4 state — `PBI_SVC` removed from roles list and from Terraform-managed-state count; §9 v0.3.0 entry — notes publish-skip explicitly.
- [x] **Shipped via PR #13** — branch `serve/skip-pbi-publish` → `master`. dbt CI 48s, terraform fmt+validate 30s, both green. Merge commit `45f81a8`. 8 files changed (+55 / −149 lines), 1 file deleted.

## End-of-day cleanup

- [x] CLAUDE.md §4 Pending: closed the v0.2.0 tag checkbox to match §9 (the tag was pushed earlier in the session at `f5d9d40`); section header renamed from "Pending" to "Done."
- [x] Verified ADR index in §8 covers 0001 → 0017 (no gaps); session logs run 2026-04-15 → 2026-04-23.

## v0.3.0 GUI build deferred to post-v1.0 (later in same session)

- [x] Decided to **defer the Power BI GUI build** (`.pbix`, `.rdl`, screenshots) to a post-v1.0 release. Same shape as ADR-0014 (Airbyte/queue deferral) — scope-cut for v1.0, pushed beyond. Rationale: the highest-impact remaining work is govern + writeup; a 2-3 hour Power BI .pbix exercise produces incremental evidence over what the design docs + Streamlit dashboards already prove.
- [x] [ADR-0018](../adr/0018-defer-power-bi-gui-build.md) written: declares `v0.3.0-serve` done at design+scaffold scope (no separate git tag — same convention as Replicate Sources at 3-tenant scope per ADR-0014). Documents the parked items (.pbix, .rdl, screenshots), reversal triggers, and what stays in v1.0 evidence (Streamlit + design docs + walkthroughs + Snowflake side).
- [x] [ADR-0017](../adr/0017-power-bi-serving.md) gains a **second addendum** at the top, parallel to the publish-skip addendum. ADR-0017 §1 + §2 design decisions remain canonical for whenever the GUI work resumes. The two-addenda pattern is intentional — both are scope cuts of the same ADR's downstream work, captured separately so the chronology reads cleanly.
- [x] CLAUDE.md updates:
  - §4 Pending v0.3.0 → renamed "Done" (declared done at design+scaffold scope per ADR-0018); GUI checkboxes removed and reappear under Parked.
  - §4 Parked → new top bullet: "Power BI GUI build (ADR-0018)" before the existing v1.1.0 / queue / shared-files bullets.
  - §6 Next milestone → rewritten to target v0.4.0-govern with concrete steps in cheapest-first order (alert → DLQ → FR_CI tighten → row access policies). Diagram updated to show govern items.
  - §8 Resolved → new entry: ADR-0018 (defer Power BI GUI build).
  - §9 phase 4 → rewritten to "declared done at design+scaffold scope (no separate tag) per ADR-0018."
  - §9 → new phase 9: "Power BI GUI build — post-v1.0 (no tag yet)" with ADR-0018 reference.
- [x] Roadmap %: phase 4 reweights from 40% complete to 100% complete at scope; new total to v1.0.0 is **~63%** (was ~57%).

## Open checkboxes — what's next

- [ ] **v0.4.0-govern** — start with the cheap items: Snowflake Alert on quarantine row-count delta, then Event Grid DLQ; then FR_CI tighten; then row access policies (biggest item, ADR-0019 worth writing for the role-to-tenant mapping).
- [ ] **v0.5.0-orchestrate-ai** — one Airflow DAG (local Docker), Cortex Document AI demo.
- [ ] **v1.0.0** — portfolio writeup (legacy vs new, cost analysis, lessons learned), top-level README polish.
- (Parked, post-v1.0) Power BI GUI build per ADR-0018; v1.1.0 Replicate operational DB per ADR-0014; user's manual onboarding of the 8 queue tenants.
