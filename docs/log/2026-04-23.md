# 2026-04-23 — v0.2.0 ship + v0.3.0 scaffold + double-deferral on Power BI

> **Retrofit note (2026-04-22):** This file was the first restructured to the
> new template in [README.md](README.md). The "Session plan" section is
> reconstructed from chat context — it captures the day's intent at start,
> not perfect ordering of every micro-decision. Sections that follow ("ADRs,"
> "Shipped + test plan," etc.) are derived from the actual narrative below.

## Roadmap context (start of day)

- **Phase entering:** v0.2.0-model-the-warehouse (build phase). Replicate Sources finalisation already shipped in PR #9 the previous evening.
- **Tag history:** v0.1.0-foundations (only).
- **% to v1.0.0:** ~50% pre-v0.2.0.
- **Active blockers / open questions:** confirm Data Vault domain (default = transactions); decide Type 2 dim_policy approach; pick whether Snowflake Semantic Views land in v0.3.x.

## Session plan (initial — pivoted twice mid-day)

Initial intent:
1. Build v0.2.0-model-the-warehouse: `dbt snapshot` for `dim_policy` + parallel `dim_policy_history`; full Vault domain on transactions (4 hubs + 3 links + 2 sats with hashdiff); tests; tag.
2. Maybe start v0.3.0-serve if time allowed.

Mid-day pivot #1: after v0.3.0 scaffold went in, decided to **skip Power BI Service publish** entirely (saved Pro license cost; nothing to demonstrate beyond what screenshots already convey).

Mid-day pivot #2 (later same session): decided to **defer the Power BI GUI build itself** to post-v1.0. Streamlit + design docs + walkthroughs become the v1.0 evidence; .pbix / .rdl move to a parked bullet.

End-of-day: doc audit + cleanup PR.

Won't-do / out of scope:
- Snowflake Semantic Views (flagged as v0.3.x stretch in ADR-0017).
- Microsoft Entra service principal auth for Power BI (flagged as future reversal trigger).
- Multiple paginated reports — one demonstrates the SSRS replacement pattern.

Risk flags considered:
- Stacked-PR merge: knew GitHub auto-closes on base-branch deletion in *theory*, didn't expect it in practice. Hit it on PR #10. Captured as ADR-0016.

ADR(s) anticipated at start of session: ADR-0015 (Vault + dim_policy snapshot). Three more got written during the day as scope decisions emerged: ADR-0016, ADR-0017, ADR-0018.

## ADRs created or updated

- **ADR-0015 (new)** — [Data Vault 2.0 on transactions, alongside the star schema; Type 2 dim_policy via dbt snapshot](../adr/0015-vault-on-transactions.md). Domain pick rationale (transactions over policies / commissions / valuations); sat-split rationale; source = `fct_transactions`; materialization with hashdiff guard for sats; parallel `dim_policy_history`.
- **ADR-0016 (new)** — [Stacked-PR merge convention for branch-protected master](../adr/0016-stacked-pr-merge-convention.md). Retarget downstream PR base to `master` *before* deleting the upstream branch. Four-line `gh` recipe documented.
- **ADR-0017 (new)** — [Power BI on Snowflake — semantic model location, connection mode, and auth](../adr/0017-power-bi-serving.md). DirectQuery for the model; Import for the paginated report. Initially with `PBI_SVC` for publish; both subsequent addenda below.
- **ADR-0017 (updated, addendum #1)** — publish skipped; `PBI_SVC` destroyed; `walkthrough/04_publish.md` deleted.
- **ADR-0017 (updated, addendum #2)** — GUI build deferred per ADR-0018; `v0.3.0-serve` declared done at design+scaffold scope.
- **ADR-0018 (new)** — [Defer Power BI GUI build to post-v1.0; v0.3.0-serve declared done at design+scaffold scope](../adr/0018-defer-power-bi-gui-build.md). Same shape as ADR-0014's Airbyte/queue deferral.

## Shipped + test plan

| PR | Title | Merge | Tag | Test plan |
|---|---|---|---|---|
| #9 | Replicate Sources finalisation: snowpipe `_LOADED_AT` fix, quarantine, mock-ops scaffold | `c2ba852` | — | dbt CI 40s pass; terraform fmt+validate 27s pass; backfill 1.8M rows verified via spot-check |
| #10 | (stacked PR for v0.2.0; auto-closed when base branch deleted) | n/a | — | n/a — see ADR-0016 |
| #11 | v0.2.0-model-the-warehouse: Type 2 dim_policy snapshot + Data Vault on transactions | `f5d9d40` | **`v0.2.0-model-the-warehouse`** | `dbt build --select snp_dim_policy dim_policy_history tag:vault` PASS=72 WARN=0 ERROR=0 in 23s; CI pass 47s |
| #12 | v0.3.0-serve scaffold: PBI_SVC user + Power BI walkthroughs + ADRs 0016/0017 | `75bb4ff` | — | `terraform apply` 2 added (PBI_SVC + grant); CI pass 48s |
| #13 | v0.3.0: skip Power BI Service publish; destroy PBI_SVC | `45f81a8` | — | `terraform apply` 2 destroyed (PBI_SVC + grant); CI pass 48s; verified `state list \| grep -i pbi` empty |
| #14 | docs: end-of-day cleanup — close v0.2.0 tag checkbox, complete 2026-04-23 log | `48b7ed3` | — | CI pass 40s |
| #15 | v0.3.0-serve declared done at design+scaffold scope; defer Power BI GUI build to post-v1.0 (ADR-0018) | `7e7c271` | — | docs only; CI pass 1m20s |

## Issues and fixes encountered

- **Stacked PR auto-closes when base branch is deleted** — promoted to [issues-and-fixes.md](issues-and-fixes.md) under a new "GitHub workflow" section with the canonical `gh` recipe. See ADR-0016 for full context.
- **`gh auth` default scopes don't include `workflow`.** Hit in the late-day addendum below: `gh repo create --push` against a tree containing `.github/workflows/*.yml` fails with `refusing to allow an OAuth App to create or update workflow ... without workflow scope`. Fix: `gh auth refresh -h github.com -s workflow` (second device-flow), then retry the push. ~5 min to resolve — below the 15-min bar for promotion to [issues-and-fixes.md](issues-and-fixes.md), noted here for chronology.

## Session log

### v0.2.0 shipped

- [x] Two commits made locally:
  - `7d6f5bc` — Replicate Sources finalisation (snowpipe `_LOADED_AT` fix, quarantine module, mock-ops scaffold, scope lock to 3 tenants)
  - `28ab685` — v0.2.0-model-the-warehouse (Type 2 `dim_policy` + Data Vault on transactions)
- [x] Branches created and pushed:
  - `replicate-sources/finalisation` (at `7d6f5bc`)
  - `core/vault-and-type2` (at `28ab685`)
- [x] Stacked PRs opened: #9 (base `master`) and #10 (base `replicate-sources/finalisation`).
- [x] **PR #9 merged with `--delete-branch`** (CI green). Merge commit `c2ba852`.
- [x] **PR #10 auto-closed by GitHub** when its base branch ceased to exist. Tried `gh pr reopen` + `gh pr edit --base master` — both rejected ("Cannot change the base branch of a closed pull request").
- [x] **Recovery:** opened PR #11 from `core/vault-and-type2` → `master` with the same body, referencing closed #10. CI green. Merged with `--delete-branch`. Merge commit `f5d9d40`.
- [x] Local `master` reset to `origin/master`. Stale local tag `v0.2.0-model-the-warehouse` (had pointed at `28ab685`) deleted.
- [x] **Tag `v0.2.0-model-the-warehouse` re-created at `f5d9d40`** (the merge commit) and pushed.

### ADR-0016 — stacked-PR merge convention

- [x] Wrote [ADR-0016](../adr/0016-stacked-pr-merge-convention.md). Retarget downstream PR base to `master` BEFORE deleting the upstream branch. Four-line `gh` recipe documented as canonical.
- [x] Cross-referenced under a new "GitHub workflow" section in [issues-and-fixes.md](issues-and-fixes.md).

### v0.3.0-serve scaffold

- [x] [ADR-0017](../adr/0017-power-bi-serving.md) — three decisions: semantic model in Power BI; DirectQuery for model + Import for paginated report; OAuth for build / `PBI_SVC` key-pair for publish.
- [x] **Snowflake side via Terraform:** `PBI_SVC` user (key-pair, `default_role = FR_ANALYST`, `default_warehouse = BI_WH`) + out-of-module grant.
- [x] `terraform plan` exactly 2 adds. `terraform apply` clean.
- [x] **Repo scaffold:** [power_bi/README.md](../../power_bi/README.md) + four walkthroughs (the publish one was deleted later in the session — see scope cut below).
- [x] **Shipped via PR #12** — branch `serve/v0.3.0-scaffold` → `master`. Merge commit `75bb4ff`.

### v0.3.0 publish-step scope cut

- [x] Decided to **skip Power BI Service publish entirely**. The .pbix + .rdl + screenshots in repo are the portfolio deliverable.
- [x] [ADR-0017](../adr/0017-power-bi-serving.md) gains a 2026-04-22 addendum at the top.
- [x] **Deleted `power_bi/walkthrough/04_publish.md`** — dead instructions.
- [x] **Trimmed `power_bi/README.md`** — dropped publish references.
- [x] **`PBI_SVC` Snowflake user destroyed via Terraform.** `terraform plan` 2 destroys. Apply clean.
- [x] **Cleanup:** removed `pbi_svc_public_key_path` from `variables.tf` + `terraform.tfvars`. Tombstone comment in `dev/main.tf`.
- [x] **Key files left on disk** at `C:/Users/Lonwabo_Eric/.snowflake/keys/pbi_svc_rsa_key.{p8,pub}`.
- [x] CLAUDE.md updated.
- [x] **Shipped via PR #13** — branch `serve/skip-pbi-publish` → `master`. Merge commit `45f81a8`.

### End-of-day cleanup

- [x] CLAUDE.md §4 Pending: closed the v0.2.0 tag checkbox to match §9; subsection renamed "Pending" → "Done."
- [x] Verified ADR index in §8 covers 0001 → 0017 (no gaps); session logs run 2026-04-15 → 2026-04-23.
- [x] **Shipped via PR #14** — branch `docs/eod-cleanup-2026-04-22` → `master`. Merge commit `48b7ed3`.

### v0.3.0 GUI build deferred to post-v1.0

- [x] Decided to **defer the Power BI GUI build** (`.pbix`, `.rdl`, screenshots) to a post-v1.0 release. Same shape as ADR-0014.
- [x] [ADR-0018](../adr/0018-defer-power-bi-gui-build.md) written: declares `v0.3.0-serve` done at design+scaffold scope (no separate git tag).
- [x] [ADR-0017](../adr/0017-power-bi-serving.md) gains a **second addendum** at the top, parallel to the publish-skip addendum.
- [x] CLAUDE.md updates: §4 Pending v0.3.0 → renamed Done; §4 Parked → new top bullet; §6 Next milestone rewritten to target v0.4.0-govern; §8 Resolved → ADR-0018 added; §9 phase 4 rewritten + new phase 9 added.
- [x] Roadmap %: phase 4 reweights from 40% complete to 100% complete at scope; new total to v1.0.0 is **~63%** (was ~57%).
- [x] **Shipped via PR #15** — branch `serve/defer-pbi-gui-build` → `master`. Merge commit `7e7c271`.

### Late-day addendum — scaffold rehearsal + memory / profile alignment

> **Retro entry.** Captured *after* execution, violating the protocol's
> plan-before-code rule. Included for chronology completeness per the
> "failure modes" clause in [SESSION_PROTOCOL.md](../SESSION_PROTOCOL.md)
> ("write a brief retro entry honestly"). Short session (<1 h), no
> commits landed in the real repo, no PRs opened, no ADRs written.

Session ran in a fresh Claude Code conversation with only memory context
loaded — no prior CLAUDE.md / protocol awareness until late in the
session. That is why plan-before-code was skipped. Noted as a lesson:
**future sessions must read CLAUDE.md §4/§6 and the previous day's Open
checkboxes before any code**, per the workflow loop in SESSION_PROTOCOL.md §"Workflow loop."

What actually happened (SAST chronology):

- [x] User invoked `/model opus` and asked to run
      [scaffold.ps1](../../../scaffold.ps1). Ran it — exited cleanly from
      the parent directory, generating a top-level scaffold tree. The
      real `snowflake-migration/` tree (from 2026-04-15 onward) was not
      overwritten; the rehearsal commit `d0c8de0` below does **not** appear
      in the production repo's `git log`.
- [x] Initial `git commit` in the scaffold failed — no git identity
      configured globally. User set `user.name=Lsilinda`, then corrected
      to `user.name=L35silinda` in the same session; `user.email=eric.silinda@gmail.com`.
- [x] Rehearsal commit `d0c8de0 Initial scaffold` landed locally in the
      rehearsal tree.
- [x] Installed **GitHub CLI 2.89.0** via `winget install --id GitHub.cli`.
      PATH updated requires a shell restart — invoked via full path
      `"C:\Program Files\GitHub CLI\gh.exe"` for the rest of the session.
- [x] `gh auth login --hostname github.com --git-protocol https --web`
      via device flow (code `1E8E-C0B8`). Authenticated as GitHub login
      **`L35ilinda`**. Default scopes granted: `gist, read:org, repo` —
      **no `workflow`**, which bit us on the first push.
- [x] **Intentional mismatch noted:** local `git config user.name` is
      `L35silinda` (with "s"); GitHub login is `L35ilinda` (no "s"). User
      confirmed; do not silently "correct" in future sessions.
- [x] `gh repo create snowflake-migration --private --source=. --remote=origin --push`.
      Repo created at <https://github.com/L35ilinda/snowflake-migration>
      (private). First push rejected — `workflow` scope missing (see
      "Issues and fixes encountered" above). Refreshed with
      `gh auth refresh -h github.com -s workflow` (code `5EF7-45D9`);
      retried push — clean.
- [x] Read [CLAUDE.md](../../CLAUDE.md) top-to-bottom at user's request
      for context alignment.
- [x] Seeded assistant-side memory: `MEMORY.md` index,
      `user_profile.md`, `project_snowflake_migration.md`.
- [x] User supplied a detailed cross-project user profile. Rewrote
      `user_profile.md` memory with structured sections (identity,
      technical background, interaction DOs/DON'Ts, decision preferences,
      learning preferences). Added identifier note flagging the L35ilinda
      vs L35silinda mismatch as intentional.
- [x] User-side: expanded MEMORY.md to index four feedback memories
      (best-practices, session-tracking protocol, Anaconda Python,
      commit-attribution) and added the "learning moment" flagging
      convention to `user_profile.md`.
- [x] User asked for this session's documentation per SESSION_PROTOCOL.md.
      Result = this addendum + the issues bullet above. No ADRs, no PRs,
      no CLAUDE.md §4 update (no project state changed).

Lesson flagged for future sessions:
- Cold-start a Claude Code session with **memory + CLAUDE.md + previous
  day's Open checkboxes** before touching anything. The protocol is
  authoritative; the assistant's memory is a pointer to it, not a
  substitute.

## Open checkboxes — what's next

- [ ] **v0.4.0-govern** — start with the cheap items: Snowflake Alert on quarantine row-count delta, then Event Grid DLQ; then FR_CI tighten; then row access policies (biggest item, ADR-0019 + ADR-0020 worth writing).
- [ ] **v0.5.0-orchestrate-ai** — one Airflow DAG (local Docker), Cortex Document AI demo.
- [ ] **v1.0.0** — portfolio writeup (legacy vs new, cost analysis, lessons learned), top-level README polish.
- (Parked, post-v1.0) Power BI GUI build per ADR-0018; v1.1.0 Replicate operational DB per ADR-0014; user's manual onboarding of the 8 queue tenants.
