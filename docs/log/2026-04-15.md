# 2026-04-15 - Foundations progress

Working session focused on getting the Foundations phase from scaffolded docs into real, runnable infrastructure.

## Tooling

- [x] Installed Terraform v1.14.8 via `winget install HashiCorp.Terraform`.
- [x] Installed Azure CLI via `winget install Microsoft.AzureCLI`.
- [x] Confirmed Windows shell environment is usable for Terraform, Azure CLI, and Snowflake setup work.

## Configuration

- [x] Created [.env](../../.env) at `snowflake-migration/.env`.
- [x] Replaced the old `.env.example` approach with a documented env block in the repo README.
- [x] Updated Snowflake programmatic auth from browser-based SSO assumptions to key-pair auth (`SNOWFLAKE_JWT`).
- [x] Generated an RSA key pair outside the repo at `C:/Users/Lonwabo_Eric/.snowflake/keys/`.
- [x] Registered the Snowflake public key on user `LSILINDA`.

## Documentation

- [x] Rewrote [README.md](../../README.md) with prerequisites, env setup, current project status, and the real immediate next step.
- [x] Created `docs/adr/` and `docs/log/` structure.
- [x] Wrote ADR-0001 through ADR-0006.
- [x] Updated [CLAUDE.md](../../CLAUDE.md) so sections 3, 4, 6, and 8 reflect the actual current state.
- [x] Updated [terraform/README.md](../../terraform/README.md) and module READMEs to match the current Terraform flow and auth model.

## Decisions (captured as ADRs)

- [x] **ADR-0001:** modular Terraform layout over flat layout
- [x] **ADR-0002:** Azure Blob remote state backend over local state
- [x] **ADR-0003:** one shared Snowflake storage integration across all three company containers
- [x] **ADR-0004:** fresh `ANALYTICS_DEV` database instead of repurposing `FSP_DATA_INTEGRATION_DB`
- [x] **ADR-0005:** key-pair auth for programmatic Snowflake access
- [x] **ADR-0006:** named RAW schemas (`RAW_MAIN_BOOK`, `RAW_INDIGO_INSURANCE`, `RAW_HORIZON_ASSURANCE`) instead of placeholder `RAW_COMPANY_NN`

## Terraform bootstrap and backend

- [x] Logged into Azure tenant `bc5006a1-0712-4769-a24f-3cc61c360e7e`, subscription `60abe083-7f78-4a57-9f4f-ca0214215c77`.
- [x] Ran `terraform init`, `plan`, and `apply` in `terraform/bootstrap/`.
- [x] Created `azurerm_storage_container.tfstate` inside existing storage account `fspsftpsource`.
- [x] Activated the Azure Blob remote backend at `fspsftpsource/tfstate/environments/dev/terraform.tfstate`.
- [x] Fixed backend access by granting the signed-in user `Storage Blob Data Contributor` on the storage account.

## Snowflake provider and auth pivot

- [x] Ran initial `terraform init` in `terraform/environments/dev/`.
- [x] Migrated the provider source from `Snowflake-Labs/snowflake` to `snowflakedb/snowflake`.
- [x] Re-initialized with the newer provider and locked on `snowflakedb/snowflake v1.2.3`.
- [x] Diagnosed failure of `authenticator = "ExternalBrowser"` as a real SAML-federation gap, not a Terraform bug.
- [x] Switched the provider config to `authenticator = "SNOWFLAKE_JWT"` plus `private_key = file(var.snowflake_private_key_path)`.
- [x] Added defensive key ignores to `terraform/.gitignore` (`*.p8`, `*.pem`, `*.key`).

## First Snowflake apply: storage integration

- [x] Applied the shared storage integration module.
- [x] Hit the provider preview-feature gate for `snowflake_storage_integration` and enabled `preview_features_enabled = ["snowflake_storage_integration_resource"]`.
- [x] Found and cleaned up drift from an earlier manual integration (`AZURE_SFTP_SOURCE_INT`).
- [x] Found a case-sensitivity issue in our naming and corrected the integration name to uppercase at the composition layer.
- [x] Re-applied and ended with `SI_AZURE_FSPSFTPSOURCE_DEV` as the Terraform-managed integration.
- [x] Learned that Snowflake reuses one account-level Azure service principal across storage integrations.

## Azure-side activation

- [x] Opened the Azure consent URL and completed admin consent.
- [x] Granted `Storage Blob Data Reader` to the Snowflake enterprise application at the storage-account scope.
- [x] Verified the integration with `DESC STORAGE INTEGRATION SI_AZURE_FSPSFTPSOURCE_DEV`.

## Database and ingest module build

- [x] Built `snowflake_database_layers` to create `ANALYTICS_DEV`, three named tenant RAW schemas, plus `STAGING`, `CORE`, and `MARTS`.
- [x] Built `snowflake_company_ingest` to create one CSV file format and one external stage per company.
- [x] Composed both modules into `terraform/environments/dev/main.tf`.
- [x] Mapped Azure containers `fsp-company-01/02/03` to `MAIN_BOOK`, `INDIGO_INSURANCE`, and `HORIZON_ASSURANCE`.
- [x] Added preview-feature opt-ins for `snowflake_stage_resource` and `snowflake_file_format_resource`.
- [x] Added outputs for `database_name`, `raw_schema_names`, and `company_stage_fully_qualified_names`.

## Current Terraform status

- [x] Checked Terraform state before the next apply: only `module.storage_integration.snowflake_storage_integration.this` is currently in state.
- [x] Re-ran `terraform init` in `terraform/environments/dev/` after adding the new module block.
- [x] Ran a fresh `terraform plan`.
- [x] Confirmed a clean plan: `13 to add, 0 to change, 0 to destroy`.
- [x] Confirmed the pending apply will create:
  - `ANALYTICS_DEV`
  - `RAW_MAIN_BOOK`, `RAW_INDIGO_INSURANCE`, `RAW_HORIZON_ASSURANCE`
  - `STAGING`, `CORE`, `MARTS`
  - `FF_CSV_COMPANY_01`, `FF_CSV_COMPANY_02`, `FF_CSV_COMPANY_03`
  - `STG_COMPANY_01_OUTBOUND`, `STG_COMPANY_02_OUTBOUND`, `STG_COMPANY_03_OUTBOUND`

## Next

- [ ] Run `terraform apply` in `terraform/environments/dev/` for the current 13-resource plan.
- [ ] Verify `LIST @ANALYTICS_DEV.RAW_MAIN_BOOK.STG_COMPANY_01_OUTBOUND/Outbound;` from a Snowflake worksheet.
- [ ] Build and apply the minimal RBAC module (`FR_ENGINEER` plus access roles).
- [ ] Build and apply `LOAD_WH` plus the first resource monitor.
- [ ] Initialize dbt and get `dbt debug` green against `ANALYTICS_DEV`.
- [ ] Configure Snowpipe for one file pattern, drop a test file, and verify RAW landing.
- [ ] Build the first dbt staging model against the RAW table.
- [ ] Decide and execute the file split across companies `01`, `02`, and `03`.
- [ ] Tag `v0.1.0-foundations`.
