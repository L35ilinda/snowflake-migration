# 2026-04-16 - Azure blob container module + onboarding queue

## Azure blob containers module

- [x] Created `terraform/modules/azure_blob_containers/` — manages containers within an existing storage account via `for_each`.
- [x] Module uses `data "azurerm_storage_account"` lookup (account is pre-existing, not Terraform-managed).
- [x] Accepts a `map(object)` of containers with configurable `access_type` (defaults to `private`).
- [x] Includes Azure naming validation (3-63 chars, lowercase alphanumeric + hyphens).
- [x] Composed in `terraform/environments/dev/main.tf` with `fsp-data-onboarding-queue` as the first managed container.

## Purpose of fsp-data-onboarding-queue

Holds files that have not yet been onboarded — i.e., files that don't have ingest pipelines, database schemas, RBAC, or layer configurations created. Acts as a staging area before full pipeline setup.

## Snowflake stage directory drift fix

- [x] Discovered `directory = "ENABLE = true"` drift on `snowflake_stage` resources: Snowflake sets it server-side but the provider treats it as `ForceNew`.
- [x] Pinned `directory = "ENABLE = true"` explicitly in `snowflake_company_ingest` module to prevent future drift.
- [x] Company 01 stage matched state; companies 02 and 03 replaced (harmless — no pipes or dependencies exist yet).

## Terraform apply

- [x] Applied: **3 added, 0 changed, 2 destroyed**.
  - Created: `fsp-data-onboarding-queue` container in `fspsftpsource`.
  - Replaced: `STG_COMPANY_02_OUTBOUND`, `STG_COMPANY_03_OUTBOUND` (directory pin).

## Descriptive container naming and ingest refactor

- [x] Adopted descriptive container names: `fsp-main-book`, `fsp-indigo-insurance`, `fsp-horizon-assurance` (replacing generic `fsp-company-01/02/03`).
- [x] Deleted legacy `fsp-company-01/02/03` containers from Azure.
- [x] Moved all files from `fsp-company-01` to `fsp-data-onboarding-queue`, then copied `main_book_*` files to `fsp-main-book`.
- [x] Flattened file layout: removed `Outbound/` subdirectory, files now at container root.
- [x] Refactored `snowflake_company_ingest` module: replaced `company_id`-based naming with descriptive `company_name`.
  - Stages: `STG_COMPANY_NN_OUTBOUND` → `STG_MAIN_BOOK`, `STG_INDIGO_INSURANCE`, `STG_HORIZON_ASSURANCE`
  - File formats: `FF_CSV_COMPANY_NN` → `FF_CSV_MAIN_BOOK`, `FF_CSV_INDIGO_INSURANCE`, `FF_CSV_HORIZON_ASSURANCE`
- [x] Renamed variables from generic (`azure_storage_container_company_02`) to descriptive (`azure_storage_container_indigo_insurance`).
- [x] Updated storage integration `allowed_locations` to reference new container names.
- [x] Hit cross-provider race condition: Snowflake validated container existence before Azure finished creating it. Fixed with `depends_on = [module.azure_containers]` on the ingest module.
- [x] Applied: **5 added, 4 changed, 3 destroyed** (across two apply runs due to race condition).
- [x] Verified `LIST @ANALYTICS_DEV.RAW_MAIN_BOOK.STG_MAIN_BOOK;` returns 6 CSVs at container root.

## RBAC module

- [x] Built `terraform/modules/snowflake_rbac/` — access-role / functional-role pattern.
- [x] Each schema gets two access roles: `AR_<DB>_<SCHEMA>_RW` (USAGE, CREATE, ALL on future tables/views) and `AR_<DB>_<SCHEMA>_RO` (USAGE, SELECT on future tables/views).
- [x] Two functional roles: `FR_ENGINEER` (all 6 RW access roles), `FR_ANALYST` (3 RO: staging, core, marts).
- [x] `LSILINDA` granted `FR_ENGINEER`. `ACCOUNTADMIN` retained — `FR_ENGINEER` is for day-to-day work, `ACCOUNTADMIN` for Terraform and account-level ops.
- [x] Applied: **78 added, 0 changed, 0 destroyed** (12 access roles + 2 functional roles + 1 user grant + 63 privilege grants).

## Warehouses + resource monitors module

- [x] Built `terraform/modules/snowflake_warehouses/` — manages warehouses, resource monitors, and USAGE grants.
- [x] Three workload-separated warehouses: `LOAD_WH` (Snowpipe/COPY), `TRANSFORM_WH` (dbt), `BI_WH` (queries). All XS, auto-suspend 60s, start suspended.
- [x] Per-warehouse resource monitors: `RM_LOAD_WH` (5 credits), `RM_TRANSFORM_WH` (3 credits), `RM_BI_WH` (2 credits). Notify at 75/90/100%, suspend at 100%, immediate at 110%.
- [x] Account-level backstop: `RM_DEV_ACCOUNT` (10 credits/month).
- [x] USAGE grants: `FR_ENGINEER` → all 3 warehouses, `FR_ANALYST` → `BI_WH` only.
- [x] Monitors wired to warehouses via reverse lookup in module (`resource_monitor` attribute on warehouse resource).
- [x] Hit `start_timestamp` error (date in the past for Snowflake's East US timezone). Fixed by setting start to `2026-05-01`.
- [x] Applied: **11 added, 0 changed, 0 destroyed**.

## dbt project initialization

- [x] Installed `dbt-snowflake` via Anaconda pip (`C:\ProgramData\anaconda3`). Executable at `C:\Users\Lonwabo_Eric\AppData\Roaming\Python\Python312\Scripts\dbt.exe`.
- [x] `dbt_project.yml` was pre-scaffolded with correct layer materializations (staging=view, intermediate=ephemeral, core=table, marts=table).
- [x] Created `dbt/profiles.yml` (gitignored) with key-pair auth (`snowflake_jwt`), `FR_ENGINEER` role, `TRANSFORM_WH` warehouse, `ANALYTICS_DEV` database.
- [x] `dbt debug` — **All checks passed!**

## Next

- [ ] Configure Snowpipe for Main Book.
- [ ] First staging model.