# 2026-04-19 - Post-Foundations housekeeping

## Doc audit

- [x] Ran a full audit of CLAUDE.md, README.md, ADRs, and session log. Flagged stale references to pre-Foundations state.
- [x] CLAUDE.md: moved RBAC/warehouses/Snowpipe/dbt items from Pending to Done; updated §4 Snowflake state with actual tables/pipes/warehouses/monitors/roles; rewrote §6 to target Replicate Sources phase; marked Foundations complete in §9.
- [x] README.md: container names updated to descriptive naming; status block rewritten to reflect Foundations complete.
- [x] ADR-0003 and ADR-0006: context notes updated to acknowledge container rename from generic to descriptive.

## Disable SFTP on fspsftpsource

- [x] Confirmed SFTP was enabled on the storage account (`isSftpEnabled: true`, `isLocalUserEnabled: true`).
- [x] No pipeline component uses SFTP — Snowpipe uses storage integration over HTTPS, local file staging uses `az storage copy`.
- [x] Ran `az storage account update --enable-sftp false --enable-local-user false`. Result: both flags `false`.
- [x] Verified stage access still works: `LIST @STG_MAIN_BOOK;` returns 7 objects.
- [x] Wrote ADR-0007 documenting the decision. Primary rationale: cost management for a portfolio project using synthetic data. Secondary: reduced attack surface.

## Architecture decision: keep STAGING layer

- [x] Discussed whether RAW → CORE directly (skipping STAGING) could simplify the model.
- [x] Decided to keep STAGING. Rationale: conformance across tenants, replay-ability from immutable RAW, separate testing boundary for source drift vs business logic, debugging trail.
- [x] Wrote ADR-0008.

## End-to-end for Indigo Insurance and Horizon Assurance

- [x] Inspected headers for all 12 files. Schemas are mostly identical to Main Book equivalents; `indigo_insurance` and `horizon_assurance` add a `ProductName` column vs `main_book_risk_benefits`.
- [x] Composed `indigo_insurance_pipes` and `horizon_assurance_pipes` module calls in `environments/dev/main.tf`. Reused existing `snowflake_snowpipe` module.
- [x] Terraform applied: **24 added** (12 landing tables + 12 pipes).
- [x] Refreshed all 12 pipes via `ALTER PIPE ... REFRESH`. All 12 tables loaded to 100K rows each (1.2M rows total).
- [x] Built 12 dbt staging models (6 per company) + source definitions + model tests.
- [x] `dbt build --select staging` — **PASS=42, WARN=40, ERROR=0**. All 18 staging views live across `stg_main_book__*`, `stg_indigo_insurance__*`, `stg_horizon_assurance__*`.

## Current pipeline state

- 18 RAW landing tables (1.8M rows total) in `RAW_MAIN_BOOK`, `RAW_INDIGO_INSURANCE`, `RAW_HORIZON_ASSURANCE`.
- 18 Snowpipes (all manual-refresh for now).
- 18 dbt staging views in `STAGING` schema.
- RBAC, warehouses, resource monitors, storage integration all unchanged.

## CORE layer — first conformed dim and fact

- [x] Added `dbt-utils` v1.3.3 via `packages.yml` for `generate_surrogate_key`.
- [x] Built `dim_advisor`: unions advisor_identifier across all 3 tenants + commission/valuation/insurance staging views, dedupes, generates `advisor_sk` hash on (company, advisor_identifier). Advisor IDs not globally unique across tenants, so the same ID in different companies is modelled as separate advisors.
- [x] Built `fct_commissions`: unions all 6 commission staging views (2 per tenant × 3 tenants). Grain = one row per commission payout. Adds `commission_sk`, `advisor_sk` FK, `company`, `commission_category` (INSURANCE/INVESTMENT).
- [x] Tests: not_null/unique on SKs, accepted_values on company + commission_category, relationships FK from fact to dim.
- [x] `dbt build --select core` — **PASS=18, WARN=0, ERROR=0**.

## Row counts

- `dim_advisor`: 1,485 advisors (Main Book 903, Indigo 289, Horizon 293)
- `fct_commissions`: 600,000 rows (100K per source table × 6 sources, balanced across tenants and categories)
- FK relationships test passed — every advisor_sk in fct resolves in dim.

## CORE: dim_product and dim_fund

- [x] Inspected source data. Key findings:
  - `product_code` never populated in commissions (0% non-null) — valuations is the only source of codes.
  - Insurance/assurance tables have `product_name` only, no code — different product universe.
  - Funds only appear in valuations. `fund_name` is the most populated key; `jse_code` only on ETFs.
- [x] Built `dim_product`: 38 rows across 3 tenants × 2 categories (INVESTMENT/INSURANCE). Main Book has investment only (risk_benefits lacks product_name).
- [x] Built `dim_fund`: 35 rows across 3 tenants. Derived `fund_type` (ETF/UNIT_TRUST) from `jse_code` presence.
- [x] Tests added for both dims.
- [x] `dbt build --select dim_product dim_fund` — **PASS=15, WARN=0, ERROR=0**.

## CORE: dim_date

- [x] Verified data ranges: earliest date = 2015-01-01 (inception_date), latest = 2025-12-28.
- [x] Built `dim_date` via `dbt_utils.date_spine`, range 2015-01-01 to 2030-12-31 (5,844 rows).
- [x] Included: `date_sk` (YYYYMMDD int), ISO calendar attributes, fiscal year/quarter (SA tax year starts 1 March), `is_weekend`, `is_month_end`, and period-start anchors.
- [x] `dbt build --select dim_date` — **PASS=7, WARN=0, ERROR=0**.

## CORE: fct_valuations

- [x] Built `fct_valuations`: unions valuations staging from all 3 tenants (300K rows total).
- [x] 4 FKs: `advisor_sk`, `product_sk`, `fund_sk`, `date_sk` — all joining back to dims successfully.
- [x] Measures: `market_value_amount` (~R335B total AUM), `units`, `monthly_income_amount`, `monthly_income_pct`.
- [x] **Grain issue encountered:** natural grain `(company, policy_number, fund_name, valuation_date)` had 7,203 collisions because source data has ~16% rows with null `policy_number` or `valuation_date`. Resolved with `ROW_NUMBER()` disambiguator per grain partition + `has_complete_grain` boolean flag for downstream lineage (84% of rows have complete grain).
- [x] `dbt build --select fct_valuations` — **PASS=9, WARN=3, ERROR=0**. All 4 FK relationship tests pass.

## CORE: dim_client

- [x] Inspected source tables. `client_id_number` only appears in valuations and transactions tables (not in commissions, risk_benefits/insurance/assurance). Commissions and insurance facts will need a bridge via `policy_number` in a later iteration.
- [x] Built `dim_client`: 15,039 unique clients across 3 tenants. Unions valuations + transactions from all companies; deduped on (company, client_id_number) with MAX for canonical attributes.
- [x] Enriched with `birth_date` and `age_years` from ID number (YYMMDDxxxxxx format, century rule: 00-29 → 2000s else 1900s). 100% parse rate; ages 27-76.
- [x] Flagged PII columns (`client_first_name`, `client_surname`, `client_id_number`, `birth_date`) in docs — to be protected with Snowflake dynamic masking policies later.
- [x] `dbt build --select dim_client` — **PASS=6, WARN=0, ERROR=0**.

## CORE: dim_policy

- [x] Investigated policy_number universe. Discovered two distinct universes per tenant: RISK (prefix RB*/IND*/HZR*, master data in risk_benefits/insurance/assurance) and INVESTMENT (prefix POL*/INV*/HZV*, only in valuations/transactions, no master data).
- [x] Built `dim_policy` with `policy_universe` column. RISK rows get full attributes (status, premiums, benefit amounts). INVESTMENT rows are minimal stubs (policy_number + FK target only).
- [x] Type 1 materialization. Type 2-ready columns included (`is_current`, `valid_from`, `valid_to`) so we can transition to dbt snapshots later without restructuring.
- [x] `dbt build --select dim_policy` — **PASS=9, WARN=0, ERROR=0**. 295,372 policies total (~92K RISK + ~6.5K INVESTMENT per tenant).
- [x] Policy status distribution is realistic (Active/Lapsed/Pending/Cancelled) — good signal for fct_policies snapshot later.

## CORE: fct_transactions, fct_policies, FK backfill, masking (4-in-1 sweep)

- [x] `fct_transactions`: 600,000 rows (300K RISK + 300K INVESTMENT). Universe-specific columns (member_id, claim_* for RISK; fund_code, units, price_per_unit for INVESTMENT) populated per source. FKs: policy_sk, client_sk, fund_sk (via dim_fund lookup), date_sk. Same grain-disambiguator pattern as fct_valuations for ~8% rows with null transaction_id.
- [x] `fct_policies`: 295,372 rows — one row per (company, policy_number) as an accumulating snapshot. Joins dim_policy to roll-ups from fct_commissions, fct_valuations (latest_market_value + avg), fct_transactions. snapshot_date = run date; Type 2-ready columns on dim_policy enable later transition to true periodic snapshots.
- [x] Backfilled `policy_sk` + `date_sk` on fct_commissions (100% coverage); `policy_sk` + `client_sk` on fct_valuations (~92% where natural keys present).
- [x] Built `snowflake_masking_policies` Terraform module. Applied 2 policies to CORE schema: `MP_MASK_STRING_PII` (returns '***MASKED***' for non-privileged roles on VARCHAR PII) and `MP_MASK_DATE_PII` (truncates birth_date to year). Attached to dim_client via dbt post-hooks on 5 columns (client_id_number, client_first_name, client_surname, client_full_name, birth_date).
- [x] Granted `FR_ANALYST` to LSILINDA so we can test masking without a second user. Privileged: ACCOUNTADMIN/FR_ENGINEER sees clear. Non-privileged: FR_ANALYST sees masked.
- [x] Full `dbt build --select core` — **PASS=89, WARN=4, ERROR=0** across 7 models and 93 tests.

## Current CORE state

| Model | Rows | Highlights |
|-------|------|------------|
| dim_advisor | 1,485 | |
| dim_product | 38 | INVESTMENT + INSURANCE |
| dim_fund | 35 | ETF/UNIT_TRUST |
| dim_date | 5,844 | SA fiscal year |
| dim_client | 15,039 | PII masked for FR_ANALYST |
| dim_policy | 295,372 | RISK + INVESTMENT, Type 2-ready |
| fct_commissions | 600,000 | FKs: advisor, policy, date |
| fct_valuations | 300,000 | FKs: advisor, product, fund, date, policy, client |
| fct_transactions | 600,000 | FKs: policy, client, fund, date |
| fct_policies | 295,372 | Accumulating snapshot across all facts |

## MARTS: first three domain marts

Created `dbt/models/marts/{finance,portfolio,risk}/` with one mart per domain to demonstrate patterns.

- [x] `finance_advisor_commissions_monthly` (91,258 rows): advisor × month × category grain. Window functions for running_net_total and YoY comparison (pct_change_yoy). Sample output: MBK0371 dropped -57% YoY in Dec 2024.
- [x] `portfolio_aum_monthly` (2,251 rows): fund × month grain. Latest-valuation-per-month methodology. MoM delta, 12-month trailing average. Top Horizon fund at R692M AUM, +17.8% MoM Dec 2024.
- [x] `risk_policy_inforce` (315 rows): company × policy_status × smoker × income_bracket. Active rate, lapse rate, annualised premium. ~190K active policies across 3 tenants, ~R82B annualised premium.
- [x] Tests: unique SKs, accepted_values on company/category, FK relationships to conformed dims.
- [x] `dbt build --select marts` — **PASS=23, WARN=0, ERROR=0**.

## Next

- [ ] More marts per domain as analytics needs emerge.
- [ ] Onboard remaining 60 files from `fsp-data-onboarding-queue`.
- [ ] Enable Snowpipe auto-ingest via Azure Event Grid.
- [ ] Consider `dbt snapshot` for dim_policy once we have dated master snapshots.
- [ ] Streamlit + Cortex Analyst apps on MARTS tables.
- [ ] Decide which CORE domain to model as Data Vault 2.0.
- [ ] Define quarantine table pattern for rejected rows.
- [ ] Enable Snowpipe auto-ingest via Azure Event Grid.