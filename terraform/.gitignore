.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Defensive: never commit private keys even if accidentally placed inside the repo.
*.p8
*.pem
*.key
